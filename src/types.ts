export interface UserContextData {
  user: {
    name: string;
    firstName: string;
    lastName: string;
    primaryEmail: string;
  };
}

export interface UserName {
  first: string;
  last: string;
}
